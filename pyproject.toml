[project]
name = "killing-decoherence"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "julia>=0.6.2",
    "jupyterlab>=4.5.4",
    "matplotlib>=3.10.7",
    "numpy>=2.3.5",
    "qiskit>=2.2.3",
    "qiskit-aer>=0.17.2",
    "qutip>=5.3.0",
    "scipy>=1.16.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/killit"]
